fn Main() {                                                          ~ IOzide doesn't require an entrypoint,
    output("The current unix timestamp in seconds is: " + time());   ~ so this output statement would also work standalone without a main function.
}